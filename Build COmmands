
// run this command to build the database for the distances of all Kerbin SOI bodies
python build_rt_netdb_expanded.py --mm "./inputs/ModuleManager.ConfigCache" --csv "./inputs/kerbin_distance_ranges.csv" --out "./outputs/rt_netdb_expanded.json"

// run this command to add al lships to the save file
python add_ships_full_run.py